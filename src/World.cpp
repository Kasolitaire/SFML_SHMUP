#include "World.h"

void World::HandleEvent(const Event& event)
{
}

World::World(RenderWindow& renderWindow) : m_renderWindowReference(renderWindow)
{
}

void World::WorldUpdate(const Time& deltaTime, const Time& totalTimeElapsed)
{
}
